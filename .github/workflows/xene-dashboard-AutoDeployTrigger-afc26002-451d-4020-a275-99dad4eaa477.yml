name: Trigger auto deployment for xene-dashboard

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/xene-dashboard-AutoDeployTrigger-afc26002-451d-4020-a275-99dad4eaa477.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.XENEDASHBOARD_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.XENEDASHBOARD_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.XENEDASHBOARD_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: xeneacr7244.azurecr.io
          registryUsername: ${{ secrets.XENEDASHBOARD_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.XENEDASHBOARD_REGISTRY_PASSWORD }}
          containerAppName: xene-dashboard
          resourceGroup: xene-rg
          imageToBuild: xeneacr7244.azurecr.io/xene-dashboard:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_



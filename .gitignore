# ── Dart & Flutter ──────────────────────────────────
.dart_tool/
.packages
.pub-cache/
.pub/
build/

# ── Generated Code ───────────────────────────────────
# Freezed/json_serializable generated files are committed so CI can run
# dart analyze without needing a build_runner step.
# Regenerate locally with: dart run build_runner build --delete-conflicting-outputs

# ── Secrets ──────────────────────────────────────────
.env
.env.staging
.env.*.local
*.secret

# ── Local staging helpers (read .env.staging; local-only) ──
packages/xene_backend/run_staging.ps1
packages/xene_backend/sim_staging.ps1

# ── OS ───────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Folders to ignore ───────────────────────────────
docs/
audit_reports/
/test/
tools/
supabase/
!supabase/
supabase/*
!supabase/migrations/
supabase/migrations/*
!supabase/migrations/20260706000000_add_soundcloud_date_evidence.sql

# Local IDE / agent state
.claude/
.dart_appdata/
.vscode/
.idea/libraries/
.idea/workspace.xml
.idea/runConfigurations/main_dart.xml
*.iml

# Flutter local metadata
.metadata
.flutter-plugins
.flutter-plugins-dependencies

# Local scratch/debug files
terminal_debug.txt
nvidia-test.json
gen_key.dart
WhySoLong.txt

# Root-level scratch Flutter app (not part of any package)
/lib/
/windows/
/assets/
devtools_options.yaml

# Generated Dart Frog server bootstrap (only the backend one is intentionally tracked)
packages/xene_app/.dart_frog/

# Session/planning docs generated during dev sessions
ARCHIVE_FEED_EXPOSURE_PLAN.md
PRESET_CURATION_PLAN.md
SESSION_CHANGELOG_*.md

.idea/modules.xml
.idea/runConfigurations/
packages/xene_app/.metadata

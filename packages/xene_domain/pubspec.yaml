name: xene_domain
description: Shared domain models and logic for Xene.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: "^3.11.5"

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0
  logging: ^1.2.0

dev_dependencies:
  build_runner: ^2.4.9
  freezed: ^3.2.0
  json_serializable: ^6.13.0
  flutter_lints: ^3.0.0
